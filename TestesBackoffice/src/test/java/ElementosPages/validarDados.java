package ElementosPages;
/*package Page;

import static org.junit.Assert.assertEquals;
import org.openqa.selenium.By;

import MetodosCore.Metodos;

public class ValidarUser extends CreateAccount {
	
	Metodos metodo = new Metodos();
		
	CreateUser user = new CreateUser();
	String usuario = user.getNome();

	public void validarUser() {
		metodo.pausa(5000, "");
		String loginName = driver.findElement(By.cssSelector("#menuUserLink > span")).getText();
		metodo.esperar(userName, "");
		System.out.println("O nome retornado �: " + loginName);
		assertEquals(usuario, loginName);
		metodo.fechar("*******************finalizando o teste com sucesso*********************");
	}
}*/